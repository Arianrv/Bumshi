# Build artifacts
/bin/
/dist/
*.exe
# Compiled server binary only — anchored so it does NOT match the source
# directory server/cmd/bumshid (an unanchored "bumshid" rule hid main.go).
/server/bumshid

# Go
server/vendor/
*.test
*.out
coverage.*

# Environment / secrets — never commit
.env
*.local
*.pem
*.key

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node / client (engine, app — later milestones)
node_modules/
dist-ssr/

# Private design/planning doc — kept local, not shipped in the public repo
DESIGN.md

# Companion browser apps — kept private; distributed as prebuilt binaries only.
# NEVER commit these, and never override with `git add -f`. See CLAUDE.md.
/app/
/app/**
/android/
/android/**

# Sandbox FUSE artifacts (never real files on your machine)
.fuse_hidden*

# Client brand assets (raw) — private
/img/
/img/**
